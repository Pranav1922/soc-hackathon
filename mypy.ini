[mypy]
python_version = 3.11

# scikit-learn ships no py.typed marker / stubs, so its submodules are untyped.
# Ignore missing imports ONLY for sklearn.* — all project code stays fully checked.
[mypy-sklearn.*]
ignore_missing_imports = True
